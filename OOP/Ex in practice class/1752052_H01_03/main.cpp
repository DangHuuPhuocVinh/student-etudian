#include "Header.h"
#include<conio.h>

 main()
{
	
	PhanSo p, p1, p2;
	int n, left, right;

	 Nhap(p);
	 RutGon(p);
	 NghichDao(p);
	 Xuat(p);
	 SoSanh(p1, p2);
	 Sum(p1, p2);
	 Multiply(p1, p2);
	 QuickSort(array[], left, right);
	 SelectionSort(array[], n);


	 _getch();
	 return 0;
	
}
