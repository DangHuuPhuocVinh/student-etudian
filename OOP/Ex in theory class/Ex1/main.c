#include "header.h"
#include<conio.h>

int main()
{
	int n;
	PhanSo ps;
	
	ps Imports(n);
	void Prints(p, n);
	T QuickSort(array[], left, right);
	void SelectionSort(array[], n);
	
	_getch();
	return 0;
}
